
# only keep users that own comments
# TODO
#query=%%account_schema%%.users:"WHERE is_guest != 1 and user_id in (select user_id from %%user_schema%%.comments@source_db)"

#query=%%account_schema%%.ACCOUNTS:"WHERE 1=1"
#query=%%account_schema%%.ACCOUNT_PROPERTIES:"WHERE 1=1"

# return 0 rows for all other tables
query="WHERE 1=2"

