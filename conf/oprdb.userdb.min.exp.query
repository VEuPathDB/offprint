# See also user comments tables processed by oprdb.comments.min.exp.query
# for sites that have comments.

# internal WDK metadata
query=%%user_schema%%.CONFIG:"WHERE 1=1"

# return 0 rows for all other tables
query="WHERE 1=2"

