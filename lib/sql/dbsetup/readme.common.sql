sql files that are duplicated between appdb and userdb. 
These should be factored out into a single instance in a common directory.

cloneDBCreation.sql

CloneRmanRestore.sql

userdb.sql and appdb.sql

lockAccount.sql

postDBCreation.sql

postScripts.sql

rmanRestoreDatafiles.sql

